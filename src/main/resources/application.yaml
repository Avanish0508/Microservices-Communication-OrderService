server:
  port: 8081
notification:
  service:
    url: http://localhost:8082

grpc:
  client:
    notification-service:
      address: 'static://localhost:9090'   # (1)
      negotiationType: plaintext           # (2) no TLS for local dev